import java.util.Scanner;public class ConsoleExercises {    public static void main(String[] args) {// 1. Write some java code that uses the variable pi to output the following:// "The value of pi is approximately 3.14."String seven = "7";        System.out.println(seven);//        double pi = 3.14159;//        System.out.format("The value of pi is approximately %.2f.", pi);// Explore the Scanner Class        Scanner scan = new Scanner(System.in);        scan.useDelimiter("\n");// 1. Prompt a user to enter a integer and store that value in an int variable using the nextInt method.//        System.out.println("Enter an integer: ");//        int intInput = scan.nextInt();//        System.out.println(intInput);//      What happens if you input something that is not an integer?        // You get a runtime error// 2. Prompt a user to enter 3 words and store each of them in a separate variable,//    then display them back, each on a newline.        //What happens if you enter less than 3 words?        //What happens if you enter more than 3 words?//        System.out.println("Enter three words");//        String userInput1 = scan.next();//        String userInput2 = scan.next();//        String userInput3 = scan.next();////        System.out.println(userInput1);//        System.out.println(userInput2);//        System.out.println(userInput3);// 3. Prompt a user to enter a sentence, then store that sentence in a String variable//    using the .next method, then display that sentence back to the user.        //do you capture all of the words?//        System.out.println("Type in a sentence: ");//        String sentenceInput1 = scan.next();//        System.out.println(sentenceInput1);// 4. Rewrite the above example using the .nextLine method.//        String sentenceInput2 = scan.nextLine();//        System.out.println(sentenceInput2);//  Calculate the perimeter and area of Codeup's classrooms// 1. Prompt the user to enter values of length and width of a classroom at Codeup.        // Use the .nextLine method to get user input and cast the resulting string to a numeric type.        // Assume that the rooms are perfect rectangles.        // Assume that the user will enter valid numeric data for length and width.        System.out.println("Enter length of the Codeup Classroom:");        int length = Integer.parseInt(scan.nextLine());//        float length = Float.parseFloat(scan.next());        System.out.println("Enter width of the Codeup Classroom:");        int width = Integer.parseInt(scan.nextLine());//        float width = Float.parseFloat(scan.next());        System.out.println("Enter height of the Codeup Classroom:");        int height = Integer.parseInt(scan.nextLine());//        float height = Float.parseFloat(scan.next());        int area = length * width; // find area//        float area = length * width; // find area        int perimeter = (length * 2) + (width * 2); // find perimeter//        float perimeter = (length * 2) + (width * 2); // find perimeter        int volume = length * width * height; // find volume//        float volume = length * width * height; // find volume// 2. Display the area and perimeter of that classroom.    // The area of a rectangle is equal to the length times the width, and the perimeter of a rectangle    // is equal to 2 times the length plus 2 times the width.        System.out.println("The area is: " + area + "."); // you can also put the calculations in here. (length * width)//        System.out.printf("The area is: %.2f. ", area); // you can also put the calculations in here. (length * width)        System.out.println("The perimeter is: " + perimeter + ".");//        System.out.printf("The perimeter is: %.2f. ", perimeter);// 3. BONUSES//    Accept decimal entries//    Calculate the volume of the rooms in addition to the area and perimeter        System.out.println("The volume is: " + volume + ".");//        System.out.printf("The volume is: %.2f. ", volume);// The Scanner class can be told specifically what characters or pattern separates tokens in a piece of input with the// useDelimiter method. Add the following line of code to your application after you have created a scanner (assuming the// variable the holds the scanner is named scanner):    //scanner.useDelimiter("\n");// How does this change the way your program operates?// Rewrite your classroom calculating program to use nextInt method. If you added the line of code above to your application,// you should now have no trouble handling multiple pieces of user input.    }}